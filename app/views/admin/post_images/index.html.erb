<div class = "container" >

  <div class = "row" >

    <div class = "col" >

      <h3 class = "mt-3">投稿一覧</h3>

      <p><%= form_tag admin_post_images_path, method: :get, class: 'boards_searchForm' do %></p>
        <p><%= select_tag :tag_id,
        options_from_collection_for_select(Tag.all,:id,:area,params[:tag_id]),
        {
          prompt: 'エリアで絞り込み検索',
          onchange: 'submit(this.form);'
        } %></p>

        <% end %>

        <%= render 'post',post_images: @post_images %>

    </div>

  </div>

</div>